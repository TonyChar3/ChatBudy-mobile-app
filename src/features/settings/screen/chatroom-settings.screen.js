import React from "react";
import { Text } from "react-native";

import { SafeArea } from "../../../components/utility/safe-area.component";

export const ChatRoomSettingsScreen = () => {
    return (
        <SafeArea>
            <Text>Chatroom settings screen</Text>
        </SafeArea>
    );
};